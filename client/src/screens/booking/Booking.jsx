import Layout from "../../components/layout/Layout";

export default function Booking(props) {
  return (
    <Layout handleDisplay={props.handleDisplay}>
      <div>
        "I am here to make your bridal makeup experience as stress free as
        possible!"
      </div>
    </Layout>
  );
}
